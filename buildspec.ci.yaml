# Do not change version. This is the version of aws buildspec, not the version of your buildspec file.
version: 0.2

env:
  variables:
    IMAGE_NAME: 'cid-checker'
    PROJECT: 'filecoin'
    ENVIRONMENT: 'mainnet'
    SUB_ENV: 'glif'
    SHORT_REGION: 'apn1'


phases:
  install:
    commands:
      - echo Environment Variables
      - export BUILD_VERSION=$(cat ./release.yaml | cut -d ':' -f2)
#      - printenv
      - nohup /usr/local/bin/dockerd --host=unix:///var/run/docker.sock --host=tcp://127.0.0.1:2375 --storage-driver=overlay2 &
      - timeout 15 sh -c "until docker info; do echo .; sleep 1; done"
  pre_build:
    commands:
      - echo Build STARTED
      - docker build -t cid-checker:$(echo $BUILD_VERSION) .
  build:
    commands:
      - echo Image built
